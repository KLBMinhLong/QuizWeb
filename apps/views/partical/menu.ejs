<nav class="navbar">
  <div class="navbar__brand">
    <a href="/" class="navbar__logo">
      <svg class="navbar__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
        stroke-linecap="round" stroke-linejoin="round">
        <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path>
        <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path>
      </svg>
      <span class="navbar__title">OnThiTracNghiem</span>
    </a>
  </div>

  <div class="navbar__links">
    <a href="/" class="navbar__link">Trang chủ</a>
    <a href="/subjects" class="navbar__link">Môn học</a>

    <% if (typeof user !=='undefined' && user) { %>
      <a href="/exam/history" class="navbar__link">Lịch sử thi</a>
      <% } %>
  </div>

  <div class="navbar__actions">
    <% if (typeof user !=='undefined' && user) { %>
      <span class="navbar__user">
        <svg class="navbar__user-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
          <circle cx="12" cy="7" r="4"></circle>
        </svg>
        <%= user.username %>
      </span>

      <% if (user.roles && user.roles.includes('admin')) { %>
        <a href="/admin" class="navbar__link navbar__link--admin">Quản trị</a>
        <% } %>

          <a href="/auth/logout" class="btn btn-secondary btn--sm">Đăng xuất</a>
          <% } else { %>
            <a href="/auth/login" class="navbar__link">Đăng nhập</a>
            <a href="/auth/register" class="btn btn-primary btn--sm">Đăng ký</a>
            <% } %>
  </div>
</nav>