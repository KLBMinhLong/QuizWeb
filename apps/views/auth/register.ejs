<%- include("../partical/header", { title: "Đăng ký" }) %>

<!-- Breadcrumb -->
<nav class="breadcrumb">
  <a href="/" class="breadcrumb__item">Trang chủ</a>
  <span class="breadcrumb__separator">›</span>
  <span class="breadcrumb__item breadcrumb__item--current">Đăng ký</span>
</nav>

<div class="admin-header" style="text-align: center">
  <h1 class="admin-header__title">Đăng ký tài khoản</h1>
</div>

<% if (error) { %>
<div class="error"><%= error %></div>
<% } %>

<div
  class="card mt-xl"
  style="max-width: 500px; margin-left: auto; margin-right: auto"
>
  <form method="post" action="/auth/register">
    <div class="form-group">
      <label class="form-label" style="color: var(--color-text-main)"
        >Tên đăng nhập</label
      >
      <input
        name="username"
        class="form-input"
        placeholder="Nhập username"
        required
        autocomplete="username"
      />
    </div>
    <div class="form-group">
      <label class="form-label" style="color: var(--color-text-main)"
        >Email</label
      >
      <input
        type="email"
        name="email"
        class="form-input"
        placeholder="Nhập email"
        required
        autocomplete="email"
      />
    </div>
    <div class="form-group" style="margin-bottom: var(--spacing-xl)">
      <label class="form-label" style="color: var(--color-text-main)"
        >Mật khẩu</label
      >
      <input
        type="password"
        name="password"
        class="form-input"
        placeholder="Nhập mật khẩu"
        required
        autocomplete="new-password"
      />
    </div>
    <button type="submit" class="btn btn-primary" style="width: 100%">
      Tạo tài khoản
    </button>
    <p
      style="
        text-align: center;
        margin-top: var(--spacing-lg);
        margin-bottom: 0;
        color: var(--color-text-muted);
        font-size: 14px;
      "
    >
      Đã có tài khoản?
      <a
        href="/auth/login"
        style="
          color: var(--color-primary);
          font-weight: 600;
          text-decoration: none;
        "
        >Đăng nhập ngay</a
      >
    </p>
  </form>
</div>

<%- include("../partical/footer") %>
